'use client';
import React from 'react';

const ErrorsPage = () => (
  <main>
    <h1>Error Logs (Dev Only)</h1>
    <p>See <code>/api/errors</code> for logs. (Integrate with logger for real logs.)</p>
  </main>
);

export default ErrorsPage;
